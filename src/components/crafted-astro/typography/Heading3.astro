---
interface Props {
    title?: string;
    subtitle?: string;
}

const { title, subtitle } = Astro.props;
---
<div class="space-y-2">
    <h3 class="text-2xl font-semibold tracking-tight">
        { !!title 
            ? title 
            : <slot /> 
        }
    </h3>
    {
        subtitle && (
            <p class="text-gray-500">
                {subtitle}
            </p>
        )
    }
</div>